# Asset tooling

generate-demo.mjs creates original procedural low/high GLBs, SVG posters and a simple Radiance HDR.
Run npm run assets:demo to regenerate them and synchronize the public demo's static directory.
The generator depends only on Three.js; no external brand asset or model download is required.
Brand-specific optimization recipes and source inventories remain with the private examples.
